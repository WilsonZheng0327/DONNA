# PlatformIO build output and downloaded toolchains
.pio/

# Per-machine secrets (copy from secrets.h.example — never commit real credentials)
firmware/*/include/secrets.h

# Dashboard
node_modules/
dist/
.env
.env.local

.DS_Store
